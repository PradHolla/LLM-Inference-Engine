# Internal working documents -- deliberately untracked.
# PROJECT.md is the private source of truth (decisions, environment, rationale).
# The public face of this repo is README.md and NOTES/predictions.md.
CLAUDE.md
NOTES/PROJECT.md
NOTES/phase4-eval-design.md
NOTES/phase5-spec-design.md
NOTES/phase6-app-design.md
NOTES/phase6a-labbench-design.md
NOTES/code-notes.md

# Python
__pycache__/
*.pyc
.venv/
venv/
.uv/

# Secrets / local overrides
.env
*.local.md

# Server-side request log, written on the GPU box
baseline/serve.jsonl

# Third-party datasets, refetchable in one line (GSM8K test split)
data/

# Raw per-event gateway/labbench traces: one line per SSE event, tens of MB per battery.
# These are a byproduct, not a result -- the per-measurement m*.jsonl files carry the
# numbers. gateway-traces-A.jsonl reached 32 MB and each battery would add another.
results/gateway-traces-A.jsonl
results/labbench-traces.jsonl
